import { IoIosArrowDropright } from "react-icons/io";

const ArrowRight = ({ size, className }) => {
  return <IoIosArrowDropright className={className} size={size} />;
};

export default ArrowRight;
